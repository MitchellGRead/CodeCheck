curl -X POST http://localhost:8000/v1/run/ -H "Content-Type: application/json" -d "{\"code\": \"print('hello, world!')\"}"
curl -X POST http://localhost:8000/v1/run/ -H "Content-Type: application/json" -d "{\"code\": \"import os; print(os.environ['HOME'])\"}"
curl -X POST http://localhost:8000/v1/run/ -H "Content-Type: application/json" -d "{\"code\": \"def calculate_departure_time(booking_time, booking_type):\\n    from datetime import timedelta\\n\\n    # Define the time to leave after the booking based on booking type\\n    if booking_type == 'ICBC':\\n        leave_time = timedelta(minutes=20)\\n    elif booking_type in ['New Patient', 'Special Needed']:\\n        leave_time = timedelta(minutes=15)\\n    elif booking_type == 'Returning Patient':\\n        leave_time = timedelta(minutes=10)\\n    else:\\n        raise ValueError('Invalid booking type')\\n\\n    # Calculate the departure time\\n    departure_time = booking_time + leave_time\\n    return departure_time\"}"

